export const autoUpdateExample = {
  code: "import { computePosition, offset, flip, shift, autoUpdate } from '/orbit'\n\nconst button = document.createElement('button')\nbutton.textContent = 'Reference'\nbutton.style.cssText = 'position: fixed; left: 50%; top: 50%; transform: translate(-50%, -50%); padding: 8px 16px;'\ndocument.body.appendChild(button)\n\nconst dropdown = document.createElement('div')\ndropdown.textContent = 'Dropdown'\ndropdown.style.cssText = 'position: fixed; background: #fff; border: 1px solid #e5e5e5; border-radius: 6px; padding: 12px 16px; box-shadow: 0 4px 12px rgba(0,0,0,.1); z-index: 1000; transform: translate3d(0,0,0); will-change: transform;'\ndocument.body.appendChild(dropdown)\n\nconst middleware = [offset(4), flip(), shift({ padding: 8 })]\n\nfunction update() {\n  const result = computePosition(button, dropdown, {\n    placement: 'bottom-start',\n    middleware,\n  })\n\n  // Custom rendering logic: transform-based positioning plus placement data attribute\n  dropdown.style.transform = `translate3d(${result.x}px, ${result.y}px, 0)`\n  dropdown.dataset.placement = result.placement\n  console.log('Positioned:', result.placement)\n}\n\nautoUpdate(button, dropdown, update)\n\nconsole.log('✓ autoUpdate with custom renderer enabled - try resizing the window')",
  name: 'autoUpdate - Track on Scroll/Resize',
};
