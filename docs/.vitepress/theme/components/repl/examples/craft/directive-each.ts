export const directiveEachExample = {
  code: "import { define, each, html, signal } from '@vielzeug/craft'\n\n// Render a keyed list that updates efficiently when items change\ndefine('demo-list', {\n  shadow: false,\n  setup() {\n    const items = signal([\n      { id: 1, name: 'Apple' },\n      { id: 2, name: 'Banana' },\n      { id: 3, name: 'Cherry' },\n    ])\n\n    const addItem = () => {\n      const next = items.value.length + 1\n      items.value = [...items.value, { id: next, name: `Item ${next}` }]\n    }\n\n    return html`\n      <button @click=${addItem}>Add item</button>\n      <ul>\n        ${each(items, (item) => item.id, (item) => html`\n          <li>${() => item.value.name}</li>\n        `)}\n      </ul>\n    `\n  },\n})\n\nconst el = document.createElement('demo-list')\ndocument.body.appendChild(el)\nconsole.log('List component mounted')",
  name: 'Keyed List with each()',
};
