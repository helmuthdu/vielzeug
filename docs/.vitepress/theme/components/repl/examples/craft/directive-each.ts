export const directiveEachExample = {
  code: "import { define, each, html, signal } from '@vielzeug/craft'\n\n// Keyed list with a fallback rendered when the list is empty\ndefine('demo-list', {\n  shadow: false,\n  setup() {\n    const items = signal([])\n\n    const addItem = () => {\n      const next = items.value.length + 1\n      items.value = [...items.value, { id: next, name: `Item ${next}` }]\n    }\n\n    const clear = () => { items.value = [] }\n\n    return html`\n      <button @click=${addItem}>Add item</button>\n      <button @click=${clear}>Clear</button>\n      <ul>\n        ${each(\n          items,\n          (item) => item.id,\n          (item) => html`<li>${() => item.value.name}</li>`,\n          () => html`<li><em>No items yet — click Add item</em></li>`,\n        )}\n      </ul>\n    `\n  },\n})\n\nconst el = document.createElement('demo-list')\ndocument.body.appendChild(el)\nconsole.log('List component mounted')",
  name: 'Keyed List with each()',
};
