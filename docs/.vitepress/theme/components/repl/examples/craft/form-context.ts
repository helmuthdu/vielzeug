export const formContextExample = {
  code: "import { createFormContext, signal } from '@vielzeug/craft'\n\n// Coordinate form state across multiple fields\nconst form = createFormContext({\n  onSubmit: async () => {\n    await new Promise((r) => setTimeout(r, 50))\n    console.log('Submitted!')\n  },\n  onReset: () => console.log('Form reset'),\n})\n\nconst emailValid = signal(false)\nconst passwordValid = signal(false)\n\nconst unregisterEmail = form.registerField(emailValid)\nconst unregisterPassword = form.registerField(passwordValid)\n\nconsole.log('valid (no fields valid):', form.valid.value)   // false\n\nemailValid.value = true\npasswordValid.value = true\nconsole.log('valid (all valid):', form.valid.value)         // true\n\nform.markDirty()\nconsole.log('dirty:', form.dirty.value)                     // true\n\n// reset() clears dirty AND the last submit error\nform.reset()\nconsole.log('dirty after reset:', form.dirty.value)         // false\nconsole.log('error after reset:', form.error.value)         // null\n\nvoid form.submit().then(() => {\n  console.log('error after successful submit:', form.error.value) // null\n  unregisterEmail()\n  unregisterPassword()\n})",
  name: 'Form Context & Validation',
};
