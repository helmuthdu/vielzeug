export const objectGetPathExample = {
  code: "import { getPath } from '@vielzeug/arsenal'\n\nconst config = {\n  server: { host: 'localhost', ports: [3000, 3001] },\n  db: { name: 'mydb', pool: { min: 2, max: 10 } }\n}\n\n// Standard dot-notation\nconsole.log(getPath(config, 'server.host'))        // 'localhost'\nconsole.log(getPath(config, 'db.pool.max'))        // 10\nconsole.log(getPath(config, 'server.ports.0'))     // 3000\nconsole.log(getPath(config, 'missing', 'default')) // 'default'\n\n// bracketNotation:true — silently converts a[0] → a.0\nconsole.log(getPath(config, 'server.ports[1]', undefined, { bracketNotation: true })) // 3001\n\n// throwOnMissing:true — throws on undefined path\ntry {\n  getPath(config, 'db.pool.timeout', undefined, { throwOnMissing: true })\n} catch (e) {\n  console.log('threw:', e.message)\n}\n\n// unsafe segments are blocked — returns defaultValue\nconsole.log(getPath(config, '__proto__.polluted', 'safe')) // 'safe'",
  name: 'getPath - Dot-notation access with bracketNotation option',
};
