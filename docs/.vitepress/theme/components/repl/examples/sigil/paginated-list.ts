export const paginatedListExample = {
  code: "import { createPaginatedList } from '@vielzeug/sigil/headless'\nimport { signal } from '@vielzeug/ripple'\n\n// createPaginatedList provides reactive page-index + sliced items.\n// pageSize can be a plain number or a reactive signal.\n\nconst items = signal([\n  'Alice', 'Bob', 'Carol', 'Dave',\n  'Eve',   'Frank', 'Grace', 'Heidi',\n  'Ivan',  'Judy',\n])\n\nconst paged = createPaginatedList({ getItems: () => items.value, pageSize: 3 })\n\nconsole.log('pageCount:', paged.pageCount.value)  // 4\nconsole.log('page 0:', paged.pageItems.value)     // ['Alice', 'Bob', 'Carol']\nconsole.log('hasNext:', paged.hasNext.value)      // true\nconsole.log('hasPrev:', paged.hasPrev.value)      // false\n\npaged.next()\nconsole.log('after next, page:', paged.pageIndex.value, paged.pageItems.value)\n// 1 → ['Dave', 'Eve', 'Frank']\n\npaged.goTo(3)\nconsole.log('page 3:', paged.pageItems.value)     // ['Judy']\nconsole.log('hasNext:', paged.hasNext.value)      // false\n\npaged.next()  // no-op on last page\nconsole.log('still page 3:', paged.pageIndex.value)\n\n// Filter shrinks the list — safePage clamps automatically\nitems.value = items.value.slice(0, 4)  // now 2 pages\nconsole.log('after filter, pageCount:', paged.pageCount.value)  // 2\nconsole.log('clamped pageIndex:', paged.pageIndex.value)        // 1 (was 3)\n\npaged.reset()\nconsole.log('after reset:', paged.pageIndex.value)  // 0",
  name: 'createPaginatedList — reactive pagination',
};
