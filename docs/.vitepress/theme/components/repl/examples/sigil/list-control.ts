export const listControlExample = {
  code: "import { createListControl, keymap } from '@vielzeug/sigil/headless'\n\n// createListControl provides keyboard-navigable list logic without any DOM.\n\nconst items = [\n  { label: 'Apple',  disabled: false },\n  { label: 'Banana', disabled: true  },  // skipped by navigation\n  { label: 'Cherry', disabled: false },\n  { label: 'Date',   disabled: false },\n]\n\nlet activeIndex = -1\n\nconst list = createListControl({\n  getIndex: () => activeIndex,\n  getItems: () => items,\n  setIndex: (i) => { activeIndex = i },\n  isItemDisabled: (item) => item.disabled,\n  getItemLabel: (item) => item.label,\n  loop: true,\n  keys: keymap('vertical'),\n})\n\nconsole.log('first():', list.first(), '→ Apple (index 0)')\nconsole.log('next():', list.next(), '→ Cherry (index 2, skips disabled Banana)')\nconsole.log('next():', list.next(), '→ Date (index 3)')\nconsole.log('next():', list.next(), '→ Apple (index 0, loops)')\n\nlist.reset()\nconsole.log('after reset, activeIndex:', activeIndex) // -1\n\n// set(-1) is equivalent to reset()\nlist.first()\nconst result = list.set(-1)\nconsole.log('set(-1) returns:', result, '| activeIndex:', activeIndex) // -1\n\n// cleanup() cancels any pending typeahead timer\nlist.cleanup()",
  name: 'createListControl — keyboard navigation',
};
