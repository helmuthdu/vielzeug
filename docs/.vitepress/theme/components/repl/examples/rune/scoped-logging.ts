export const scopedLoggingExample = {
  code: "import { Rune } from '/rune'\n\n// scope() extends namespace without mutating the parent\nconst api = Rune.scope('api')\nconst auth = api.scope('auth')\n\nconsole.log('root namespace:', JSON.stringify(Rune.config.namespace)) // ''\nconsole.log('api  namespace:', api.config.namespace)                  // 'api'\nconsole.log('auth namespace:', auth.config.namespace)                 // 'api.auth'\n\n// withBindings() pins fields to every call\nconst reqLog = auth.withBindings({ requestId: 'r-001', userId: 'u-42' })\nreqLog.info('token check')\nreqLog.warn({ expired: false }, 'token refreshed')\n\nconsole.log('bindings snapshot:', reqLog.bindings)\n\n// child() inherits transports; apply a level override\nconst verbose = api.child({ logLevel: 'debug' })\nconsole.log('verbose level:', verbose.config.logLevel)\n\nconsole.log('(Open DevTools console to see styled output)')",
  name: 'Scoped Loggers',
};
