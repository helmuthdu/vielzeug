export const scopedLoggingExample = {
  code: "import { Rune } from '@vielzeug/rune'\n\n// child() dot-joins namespaces automatically\nconst api = Rune.child({ namespace: 'api' })        // 'api'\nconst auth = api.child({ namespace: 'auth' })        // 'api.auth'\nconst orders = api.child({ namespace: '/orders' })   // 'orders' (absolute — / prefix)\n\nconsole.log('root:', JSON.stringify(Rune.config.namespace)) // ''\nconsole.log('api:', api.config.namespace)                  // 'api'\nconsole.log('auth:', auth.config.namespace)                 // 'api.auth'\nconsole.log('orders:', orders.config.namespace)             // 'orders'\n\n// withBindings() pins fields to every call\nconst reqLog = auth.withBindings({ requestId: 'r-001', userId: 'u-42' })\nreqLog.info('token check')\nreqLog.warn({ expired: false }, 'token refreshed')\n\nconsole.log('bindings snapshot:', reqLog.bindings)\n\n// setLevel() + resetLevel() for runtime debug toggling\napi.setLevel('debug')\nconsole.log('debug enabled:', api.enabled('debug')) // true\napi.resetLevel()\nconsole.log('after reset:', api.config.logLevel)    // original level\n\nconsole.log('(Open DevTools console to see styled output)')",
  name: 'Scoped Loggers',
};
