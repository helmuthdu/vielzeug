export const autoUpdateExample = {
  code: "import { positionFloat, offset, flip, shift, autoUpdate } from '@vielzeug/floatit'\n\nconst button = document.createElement('button')\nbutton.textContent = 'Reference'\nbutton.style.cssText = 'position: fixed; left: 50%; top: 50%; transform: translate(-50%, -50%); padding: 8px 16px;'\ndocument.body.appendChild(button)\n\nconst dropdown = document.createElement('div')\ndropdown.textContent = 'Dropdown'\ndropdown.style.cssText = 'position: fixed; background: #fff; border: 1px solid #e5e5e5; border-radius: 6px; padding: 12px 16px; box-shadow: 0 4px 12px rgba(0,0,0,.1); z-index: 1000;'\ndocument.body.appendChild(dropdown)\n\nasync function update() {\n  const result = await positionFloat(button, dropdown, {\n    placement: 'bottom-start',\n    middleware: [offset(4), flip(), shift({ padding: 8 })],\n  })\n  console.log('Positioned:', result.placement)\n}\n\nconst cleanup = autoUpdate(button, dropdown, update)\nawait update()\n\nconsole.log('✓ autoUpdate tracking enabled - try resizing the window')",
  name: 'autoUpdate - Track on Scroll/Resize',
};
