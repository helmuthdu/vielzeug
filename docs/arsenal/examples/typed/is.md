---
title: 'Arsenal — is (not exported)'
description: 'is namespace is not part of the Arsenal public API.'
---

# is

Arsenal does not export an `is` namespace. Individual typed predicates are exported at the top level — see the [Typed Predicates examples](./../typed.md).
