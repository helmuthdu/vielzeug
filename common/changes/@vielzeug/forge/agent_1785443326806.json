{
  "changes": [
    {
      "comment": "refactor(forge) root and scoped forms now share one implementation (createFormView); removed submitOrThrow/ForgeValidationError (use submit()'s SubmitResult), validate(name)/validate(fields[]) overloads (use validateFields), for-await iteration (use subscribe), FieldState.hasError (use error !== undefined)",
      "packageName": "@vielzeug/forge",
      "type": "minor"
    }
  ],
  "email": "agent@vielzeug",
  "packageName": "@vielzeug/forge"
}
