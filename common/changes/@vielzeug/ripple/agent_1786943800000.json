{
  "changes": [
    {
      "comment": "fix: computed nodes recover from first-run derivation failures — collect() commits partial dependencies when observer.dependencies is empty so a later source change can trigger refresh; ComputedNode.get value tracks itself before refresh so a consumer that reads a failing computed still depends on it and re-runs when the computed recovers",
      "packageName": "@vielzeug/ripple",
      "type": "patch"
    }
  ],
  "email": "agent@vielzeug",
  "packageName": "@vielzeug/ripple"
}
