{
  "changes": [
    {
      "comment": "refactor: remove SandboxError.is() static type guard (use instanceof SandboxError); delete _sandbox.ts re-export hub and move buildCsp to _policy.ts; inline buildRuntimeDocument wrapper into buildDocumentFromOptions; sort SandboxMessage union members alphabetically to remove eslint-disable suppressions",
      "packageName": "@vielzeug/sandbox",
      "type": "minor"
    }
  ],
  "email": "agent@vielzeug",
  "packageName": "@vielzeug/sandbox"
}
