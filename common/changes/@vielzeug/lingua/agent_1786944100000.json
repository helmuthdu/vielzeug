{
  "changes": [
    {
      "comment": "refactor: deduplicate MessageKey, TextKey, and PluralKey recursive types into a single generic CatalogPaths<C, Leaf> helper — same type-level behavior, one source of truth for traversal logic",
      "packageName": "@vielzeug/lingua",
      "type": "patch"
    },
    {
      "comment": "refactor: export isPluralMessage from _catalog.ts and reuse in validate.ts — removes duplicated type guard",
      "packageName": "@vielzeug/lingua",
      "type": "patch"
    },
    {
      "comment": "feat: add compareCatalogs(catalogs) to /validate subpath — compares key parity across locales, reporting missing and extra keys relative to the first locale as base; catches the most common i18n defect of incomplete translations",
      "packageName": "@vielzeug/lingua",
      "type": "minor"
    }
  ],
  "email": "agent@vielzeug",
  "packageName": "@vielzeug/lingua"
}
