/**
 * Shared TypeScript types, prop bundles, and design-size presets for Sigil components.
 *
 * Re-exports from three focused modules:
 * - `./types`       — primitive type aliases and derived form-prop types
 * - `./prop-bundles` — reusable craft prop bundle objects
 * - `./size-presets` — sizeVariantMixin size preset constants
 *
 * `src/types/` re-exports from this file for the public `@vielzeug/sigil/types` entry point.
 */

export * from './prop-bundles';
export * from './size-presets';
export * from './types';
