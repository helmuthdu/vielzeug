// Removed — no throw sites existed in the sandbox package.
// Error subclasses (SandboxDisposedError, SandboxTimeoutError) were speculative
// API surface. Callers receive dev warnings via console.warn instead.
