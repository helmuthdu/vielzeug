// Re-exported from shared/config.ts — the authoritative source for all BuildIt shared types.
// This file exists for backward-compatible imports within the package.
export * from '../shared/config';
